-- Represents a File prefix of suffix.
-- This is used in file name construction rules.
-- This can be a prefix (before a base name) or suffix (after a base name).
CREATE TYPE [AppGeneral].[uddtFileAffix] FROM NVarchar(50)
